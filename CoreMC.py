# Amount will be inputed by the user.

def fibonate(amount):
    for num in range(amount):
        fibo = []
        fibo.append(num)
        num += 1
        print(fibo)

# Still Learning to deal with Lists.